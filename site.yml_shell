---
- name: Test shell command 1
  hosts: rally1
  tags: always
  tasks:
    - shell: "touch /root/test_with_quote" 


- name: Test shell command 2
  hosts: rally1
  tags: always
  tasks:
    - shell: touch /root/test_without_quote
